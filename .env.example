# Copy this file to .env and adjust values for your environment
# XAMPP users don't need this file, defaults in config.php work out of the box

DB_HOST=db
DB_USER=jsonconv
DB_PASS=jsonconv123
DB_NAME=json_converter
DB_ROOT_PASS=rootpass123
APP_URL=http://localhost:8080/json_converter
