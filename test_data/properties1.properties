# Application settings
app.name=JSON Converter
app.ver=1.0.0
app.env=production
app.debug=false

# Database
db.host=localhost
db.port=3306
db.name=converter_db

# Server
server.host=0.0.0.0
server.port=8080
server.timeout=30
